<div class="main">
  <div class="container">
    <h2 class="main_h2">新規会員登録</h2>
    <div class="main_inner">
      <div class="row">
        <div class="offset-md-1 col-md-10">
          <p>以下の項目を全て入力して「新規会員登録する」ボタンをクリックしてください。<br/>すでに会員登録をしている方は<%= link_to "こちらからログイン", new_user_session_path %>できます。</p>

          <%= devise_error_messages %>

          <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

            <table class="table table-bordered">
              <tbody>
                <tr>
                  <th>
                    <%= f.label :username %><br />
                  </th>
                  <td>
                    <span class="type">半角英数字で30文字以内</span>
                    <%= f.text_field :username, autofocus: true, autocomplete: "username", class: "form-field" %>
                    <span class="notice">※サイト内IDとしても利用します。</span>
                  </td>
                </tr>
                <tr>
                  <th>
                    <%= f.label :email %><br />
                  </th>
                  <td>
                    <%= f.email_field :email, autocomplete: "email", class: "form-field" %>
                  </td>
                </tr>
                <tr>
                  <th>
                    <%= f.label :password %>
                  </th>
                  <td>
                    <span class="type">半角英数字で6文字以上</span>
                    <%= f.password_field :password, autocomplete: "password", class: "form-field"  %>
                  </td>
                </tr>
                <tr>
                  <th>
                    <%= f.label :password_confirmation %><br />
                  </th>
                  <td>
                    <span class="type">半角英数字で6文字以上</span>
                    <%= f.password_field :password_confirmation, class: "form-field"  %>
                  </td>
                </tr>
              </tbody>
            </table>

            <div class="btn_center">
              <%= f.submit "新規会員登録する", class: "btn" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
